смотри, изучаю langchain4j, изучил базовую работу с настройкой и написанием своей нейросети, теперь хочу сделать проект связанный с оркестрацией агентов. тоесть чтобы был оркестратор который управляет другими агентами. сделаем один оркестратор, 3 агента. 1 агент у нас будет отвечать за поиск информации, второй будет отвечать за кодинг и написание продукта, третий будет смотреть на работу второго и писать ему ревью, по которому в случае чего второй будет переделывать работу. для подключения моделей будем использовать openrouter, модель для кодинга будет glm 5.2, для ревью думаю gptmini - 4.1, для ресерча и поиска gemini 2.5

Идея очень хорошая именно как учебный проект. Ты изучишь сразу несколько важных концепций LangChain4j:

Agent Orchestration
Multi-Agent Systems
Routing
State Management
Structured Output
Memory
Tool Calling
Retry Loop

Я бы только немного изменил архитектуру, чтобы она была ближе к тому, как подобные системы строят в реальных продуктах.

Архитектура
                    User
                      │
                      ▼
             ┌─────────────────┐
             │   Orchestrator  │
             └─────────────────┘
                 │      │
      ┌──────────┘      └─────────────┐
      ▼                               ▼
┌────────────┐                 ┌─────────────┐
│ Researcher │                 │   Coder     │
└────────────┘                 └─────────────┘
                                      │
                                      ▼
                              ┌─────────────┐
                              │ Code Reviewer│
                              └─────────────┘
                                      │
                    approve ◄──────────┴─────────► reject
                                      │
                                      ▼
                                   Coder

То есть Review не общается с пользователем вообще.

Он общается только с Coder.

Оркестратор лишь принимает решение, когда завершить цикл.

Поток работы

Например пользователь пишет

Сделай REST API для Todo приложения.

Оркестратор делает следующее.

Шаг 1

Передает задачу Research агенту.

Research собирает:

Spring Boot 3.5

Лучшие практики

REST conventions

DTO

Repository

Controller

Service

Validation

...

и возвращает большой отчет.

Шаг 2

Оркестратор объединяет

User Request

+

Research Result

и отправляет это Coder.

Шаг 3

Coder пишет

структуру проекта

pom.xml

controller

service

repository

entity

README
Шаг 4

Оркестратор передает результат Reviewer.

Reviewer отвечает строго JSON.

Например

{
  "approved": false,
  "score": 74,
  "feedback": [
    "Добавить валидацию DTO",
    "Нет обработки ошибок",
    "Repository возвращает Optional без проверки"
  ]
}
Шаг 5

Оркестратор смотрит

approved == false

и снова вызывает Coder.

Но теперь вместе с предыдущим кодом и замечаниями.

Исправь следующие замечания:

1 ...
2 ...
3 ...
Шаг 6

Reviewer снова проверяет.

Повторять максимум

3-5 итераций

После этого вернуть пользователю результат.

Почему именно Оркестратор принимает решения

Очень распространенная ошибка —

делать так

Reviewer
   │
   ▼
Coder

где Reviewer сам вызывает Coder.

Так лучше не делать.

Лучше чтобы только Оркестратор управлял процессом.

Получается DAG.

User
   │
   ▼
Orchestrator
   │
   ├────► Research
   │
   ├────► Coder
   │
   └────► Reviewer

Это значительно проще масштабировать.

Какие модели использовать

Я бы выбрал примерно так.

Research
Gemini 2.5 Flash

или

Gemini 2.5 Pro

Причина

очень хороший анализ
длинный контекст
дешево
Coding
GLM 5.2

Отличный выбор.

Он действительно очень хорошо пишет Java.

Review

Я бы использовал

GPT-4.1 Mini

Потому что

хорошо следует инструкциям
стабильно выдает JSON
достаточно строгий
Что должен хранить Оркестратор

Очень желательно сделать объект состояния.

Например

public class TaskContext {

    private String userRequest;

    private String research;

    private String generatedCode;

    private ReviewResult review;

    private int iteration;

}

Именно его передавать между агентами.

Получается очень чистая архитектура.

Как должны выглядеть агенты

Например общий интерфейс

public interface Agent<I, O> {

    O execute(I input);

}

Тогда

ResearchAgent
    execute(TaskContext)

CoderAgent
    execute(TaskContext)

ReviewerAgent
    execute(TaskContext)

Все очень единообразно.

Оркестратор

Псевдокод

context = new TaskContext();

context.userRequest = userInput;

context.research = researchAgent.execute(context);

context.generatedCode = coderAgent.execute(context);

while (true) {

    context.review = reviewerAgent.execute(context);

    if (context.review.approved())
        break;

    context.generatedCode = coderAgent.execute(context);

    context.iteration++;

    if (context.iteration >= 3)
        break;
}

return context.generatedCode;
Что использовать из LangChain4j

Я бы специально попробовал задействовать несколько ключевых возможностей библиотеки:

AiServices — для каждого агента как отдельного AI-сервиса с собственным системным промптом.
ChatModel — создать отдельный экземпляр для каждой модели (через OpenRouter), чтобы легко назначать разные LLM разным агентам.
@SystemMessage и @UserMessage — четко разделить роль агента и входные данные.
Structured Output (например, через POJO) — особенно для ответа Reviewer, чтобы получать объект ReviewResult, а не разбирать JSON вручную.
ChatMemory — при желании можно сохранить историю только для Coder, чтобы он видел предыдущие итерации и замечания.
Tools — на следующем этапе можно дать Research-агенту доступ к поисковому инструменту или базе знаний.
Возможное развитие проекта

После первой рабочей версии можно постепенно усложнять систему, не переписывая архитектуру:

Planner Agent — сначала разбивает задачу на подзадачи, а уже затем подключаются Research и Coder.
Технический архитектор — проектирует структуру проекта до начала кодирования.
QA Agent — генерирует тест-кейсы и проверяет покрытие.
Test Runner — если подключить локальный инструмент, агент сможет запускать тесты и анализировать ошибки.
Documentation Agent — автоматически пишет README.md, JavaDoc и документацию по API.
Несколько Reviewer — например, один оценивает архитектуру, второй — стиль кода, третий — безопасность. Оркестратор объединяет их вывод перед следующей итерацией.

Такой проект уже будет очень близок к архитектуре современных multi-agent систем. Он остается достаточно компактным для учебных целей, но при этом демонстрирует ключевые паттерны: централизованную оркестрацию, разделение ответственности между агентами, структурированный обмен данными и итеративный цикл "генерация → ревью → исправление". Это станет хорошей базой для последующего знакомства с более сложными решениями, такими как LangGraph или распределенные агентные системы.

Хорошо. Теперь пишем OpenRouterConfig.

Задача этого класса:

Подключиться к OpenRouter.
Создать 3 независимые модели:
Research → Gemini 2.5 Flash
Coding → GLM 5.2
Review → GPT-4.1 Mini
Отдать их Spring Context как Bean.
Далее эти модели будут внедряться в соответствующие агенты.
Файл
config/OpenRouterConfig.java
package com.example.multiagent.config;


import dev.langchain4j.model.chat.ChatLanguageModel;
import dev.langchain4j.model.openai.OpenAiChatModel;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;


@Configuration
public class OpenRouterConfig {


    @Value("${openrouter.api-key}")
    private String apiKey;


    @Value("${openrouter.base-url}")
    private String baseUrl;


    @Value("${models.research}")
    private String researchModel;


    @Value("${models.coding}")
    private String codingModel;


    @Value("${models.review}")
    private String reviewModel;



    /**
     * Модель для Research агента
     */
    @Bean(name = "researchModel")
    public ChatLanguageModel researchModel() {


        return OpenAiChatModel.builder()

                .apiKey(apiKey)

                .baseUrl(baseUrl)

                .modelName(researchModel)

                .temperature(0.2)

                .build();
    }



    /**
     * Модель для Coding агента
     */
    @Bean(name = "codingModel")
    public ChatLanguageModel codingModel() {


        return OpenAiChatModel.builder()

                .apiKey(apiKey)

                .baseUrl(baseUrl)

                .modelName(codingModel)

                .temperature(0.1)

                .maxTokens(8000)

                .build();
    }



    /**
     * Модель для Review агента
     */
    @Bean(name = "reviewModel")
    public ChatLanguageModel reviewModel() {


        return OpenAiChatModel.builder()

                .apiKey(apiKey)

                .baseUrl(baseUrl)

                .modelName(reviewModel)

                .temperature(0.0)

                .build();
    }

}
Что здесь происходит
Research
.temperature(0.2)

Почему низкая температура?

Потому что задача исследователя:

собрать факты
структурировать информацию
не фантазировать
Coding
.temperature(0.1)

Для генерации кода нужна стабильность.

Мы хотим:

один запрос → похожий результат.

Review
.temperature(0.0)

Ревьюер должен быть максимально детерминированным.

Он должен отвечать:

approved = true

или

approved = false

а не писать творческие рассуждения.

Важный момент

Все три модели имеют один тип:

ChatLanguageModel

Но Spring различает их по имени:

@Bean(name = "researchModel")
@Bean(name = "codingModel")
@Bean(name = "reviewModel")

Дальше, например, в CodingAgent мы будем писать:

@Autowired
@Qualifier("codingModel")
private ChatLanguageModel model;

или через конструктор:

public CodingAgent(
 @Qualifier("codingModel")
 ChatLanguageModel model
)
Небольшое улучшение

В дальнейшем я бы заменил @Value на @ConfigurationProperties.

То есть вместо:

@Value("${models.coding}")
private String codingModel;

сделать:

@ConfigurationProperties(prefix="models")
public class ModelProperties {

    private String research;
    private String coding;
    private String review;

}

Это более "Spring way".

Но для первого шага @Value нормально.

Следующим шагом пишем model слой:

TaskContext.java — состояние всей задачи между агентами.
ReviewResult.java — структурированный ответ ревьюера.

Именно эти два класса будут связывать всех агентов между собой.

давай, продолжаем писать

Продолжаем. Теперь создаём model слой.

Это фундамент всей системы. Все агенты будут работать не напрямую друг с другом, а через общий объект состояния TaskContext.

Идея:

User Request
      |
      v
TaskContext
      |
      ├── ResearchAgent добавляет researchReport
      |
      ├── CodingAgent добавляет generatedCode
      |
      └── ReviewAgent добавляет reviewResult
1. TaskContext.java

Файл:

model/TaskContext.java

Создаем объект, который будет путешествовать через весь pipeline.

package com.example.multiagent.model;


import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;


@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class TaskContext {


    /**
     * Оригинальный запрос пользователя
     */
    private String userRequest;



    /**
     * Ответ исследовательского агента
     */
    private String researchReport;



    /**
     * Код, который сгенерировал Coding Agent
     */
    private String generatedCode;



    /**
     * Последний результат ревью
     */
    private ReviewResult reviewResult;



    /**
     * Количество итераций исправления
     */
    @Builder.Default
    private int iteration = 0;



    /**
     * Проверяем, закончена ли задача
     */
    public boolean isApproved() {

        return reviewResult != null
                &&
                reviewResult.isApproved();
    }


}
Почему именно такие поля
userRequest

Например:

Создай REST API для интернет-магазина

Всегда сохраняем исходный запрос.

researchReport

После ResearchAgent:

Использовать Spring Boot 3

Создать Entity Product

Добавить DTO слой

Использовать PostgreSQL
generatedCode

После CodingAgent:

package com.shop.controller;

@RestController
...
reviewResult

После Reviewer:

{
 "approved":false,
 "score":75,
 "feedback":[
    "Нет обработки ошибок",
    "Добавить validation"
 ]
}
iteration

Нужен для защиты от бесконечного цикла:

Coder
 ↓
Review
 ↓
Coder
 ↓
Review
 ↓
Coder
 ↓
Review

Например:

if(iteration >= 3)
    stop;
2. ReviewResult.java

Файл:

model/ReviewResult.java

Это очень важный класс.

Мы будем использовать его для Structured Output LangChain4j.

То есть модель сама вернет JSON, который автоматически превратится в Java объект.

package com.example.multiagent.model;


import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;


import java.util.List;


@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ReviewResult {


    /**
     * Можно ли принимать код
     */
    private boolean approved;



    /**
     * Оценка качества от 0 до 100
     */
    private int score;



    /**
     * Список замечаний
     */
    private List<String> feedback;



    /**
     * Краткое объяснение ревьюера
     */
    private String summary;


}

Теперь результат работы Reviewer будет выглядеть так:

{
  "approved": false,
  "score": 82,
  "feedback": [
    "Добавить обработчик исключений",
    "Использовать DTO вместо Entity напрямую"
  ],
  "summary": "Архитектура хорошая, но требуется улучшение обработки ошибок"
}

И LangChain4j сможет получить:

ReviewResult result;

без ручного парсинга.

3. Добавляем AgentResponse.java

Я бы еще добавил общий ответ наружу.

Файл:

model/AgentResponse.java

Он нужен для REST контроллера.

package com.example.multiagent.model;


import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;


@Data
@Builder
@AllArgsConstructor
public class AgentResponse {


    private String status;


    private int iterations;


    private String result;


}

Теперь у нас есть состояние:

              TaskContext

                 |
                 |
    +------------+------------+
    |            |            |
Research     Coding       Review


              |
              v

        AgentResponse
Текущий прогресс проекта

Готово:

✅ Spring Boot структура
✅ OpenRouter конфигурация
✅ 3 модели LLM
✅ TaskContext
✅ ReviewResult
✅ AgentResponse

Следующий этап — самое интересное:

Создаем SystemPrompts.java

Там будут личности наших агентов:

Researcher:

Ты эксперт по анализу технологий...

Coder:

Ты senior Java developer...

Reviewer:

Ты строгий code reviewer...

После этого перейдем к самим интерфейсам ResearchAgent, CodingAgent, ReviewAgent через AiServices LangChain4j.

привет, проанализируй это и продолжим работу

Изучил. В целом мне нравится направление — для учебного проекта это уже не "очередной чат с LLM", а полноценная multi-agent система с четким разделением ответственности.

Что уже хорошо продумано:

✅ Центральный Orchestrator, который один управляет всем пайплайном.
✅ Общий TaskContext, который передается между агентами.
✅ Разные модели под разные задачи.
✅ Reviewer возвращает структурированный объект (ReviewResult), а не свободный текст.
✅ Ограничение числа итераций ревью.
✅ Использование AiServices, что соответствует философии LangChain4j.

Но я бы предложил сразу немного улучшить архитектуру, пока проект еще маленький. Это избавит от рефакторинга позже.

1. Не передавать весь TaskContext каждому агенту

Сейчас получается так:

Research.execute(TaskContext)

Coder.execute(TaskContext)

Reviewer.execute(TaskContext)

Но Research вообще не должен знать про:

generatedCode
reviewResult
iteration

Лучше каждому агенту давать только то, что ему действительно нужно.

Например:

ResearchInput

- userRequest
CodingInput

- userRequest
- researchReport
- previousCode
- reviewFeedback
ReviewInput

- userRequest
- generatedCode

А уже Orchestrator собирает эти объекты.

Так ответственность агентов станет намного чище.

2. Сделать единый интерфейс Agent

Ты уже предлагал его:

public interface Agent<I, O> {

    O execute(I input);

}

Я бы обязательно его оставил.

Получится:

ResearchAgent
    implements Agent<ResearchInput, ResearchResult>

CodingAgent
    implements Agent<CodingInput, CodingResult>

ReviewerAgent
    implements Agent<ReviewInput, ReviewResult>

Потом можно добавить любые новые агенты вообще без изменения архитектуры.

3. SystemPrompt не хранить в классе с константами

Сейчас планируется

SystemPrompts.java

с кучей строк.

Я бы не делал так.

Лучше:

resources/prompts/

research-system.txt

coder-system.txt

reviewer-system.txt

Потом просто читать:

ResourceLoader

или

ClassPathResource

Почему?

Потому что промпты будут постепенно расти до 100–300 строк.

Хранить их в Java-файлах неудобно.

4. ReviewResult

Я бы немного изменил.

Сейчас:

boolean approved;

int score;

List<String> feedback;

String summary;

Я бы добавил:

List<String> strengths;

Например

strengths

- Хорошая структура проекта
- Использован Builder

Это позволит Coder понимать, что менять не нужно.

5. CodingAgent

Я бы сразу предусмотрел два режима работы.

Не только

Generate

но и

Improve

Например

если generatedCode == null

↓

Создай проект

иначе

Исправь этот код.

Замечания:

...

Это сильно упростит промпт.

6. Итерации

Сейчас

iteration

это просто число.

Я бы сделал

List<ReviewResult> reviewHistory;

Тогда можно будет посмотреть

Iteration 1

↓

75

↓

Iteration 2

↓

84

↓

Iteration 3

↓

95

Это потом пригодится даже для UI.

7. Самая важная вещь — разделить AI и бизнес-логику

Это ошибка, которую многие допускают.

Не стоит делать так:

CodingAgent

↓

сам решает

↓

если review плохой

↓

снова вызывает модель

Правильно:

Orchestrator

↓

Coder

↓

Reviewer

↓

Orchestrator принимает решение

↓

Coder

То есть агент никогда не вызывает другого агента.

Он только выполняет свою задачу.

8. LangChain4j

Я бы задействовал еще одну возможность библиотеки.

Вместо

String generatedCode;

можно сделать

class CodingResult {

    private String code;

    private String explanation;

}

Тогда Coder будет отдавать структурированный результат.

Это ближе к реальным production-системам.

Предлагаемый план разработки

Я бы двигался в таком порядке:

✅ Конфигурация моделей (OpenRouterConfig)
✅ Модели (TaskContext, ReviewResult и т.д.)
Промпты агентов (resources/prompts/*.txt)
Интерфейсы AiServices (ResearchAgent, CodingAgent, ReviewerAgent)
OrchestratorService с циклом Research → Code → Review → Improve
REST-контроллер для запуска процесса
Подключение реального веб-поиска для Research через Tools
Логирование каждого этапа и истории итераций

Сейчас мы как раз подошли к одному из самых важных этапов — созданию AiServices. Именно здесь станет видно, как правильно организовать системные промпты, структурированные ответы и привязку разных моделей к разным агентам через LangChain4j. Я бы начал именно с этого, а уже затем реализовал OrchestratorService.

Отличный выбор. Для нашего проекта Tavily подходит практически идеально:

простой REST API;
бесплатного тарифа достаточно для учебного проекта;
результаты оптимизированы для LLM;
позже его легко заменить на Exa или другой поисковик, не меняя архитектуру агентов.

Я бы сделал интеграцию так, чтобы ResearchAgent вообще не знал, откуда берутся данные. Он будет работать только с SearchTool, а SearchTool уже использует TavilyClient.

Предлагаемая структура
src/main/java/com/example/multiagent
│
├── agent
│
├── config
│
├── integration
│   └── tavily
│       ├── TavilyClient.java
│       ├── TavilyConfig.java
│       ├── dto
│       │   ├── TavilyRequest.java
│       │   ├── TavilyResponse.java
│       │   └── TavilyResult.java
│       │
│       └── exception
│           └── TavilyException.java
│
├── tool
│   └── SearchTool.java
│
└── ...

Такое разделение соответствует принципу Separation of Concerns:

integration — работа с внешними API;
tool — инструменты, доступные LLM;
agent — AI-сервисы;
service — оркестрация.
План интеграции

Я бы разбил работу на несколько небольших шагов:

Этап 1

✅ Настроить application.yml

tavily:
  api-key: ${TAVILY_API_KEY}
  base-url: https://api.tavily.com
Этап 2

Создать DTO:

TavilyRequest
TavilyResponse
TavilyResult
Этап 3

Создать TavilyClient

Он будет отвечать только за HTTP-запросы:

search(query)
        │
        ▼
 POST /search
        │
        ▼
 TavilyResponse

Никакой логики агентов внутри него не будет.

Этап 4

Создать SearchTool

Именно этот класс будет использовать LangChain4j.

@Tool
public String search(String query)

Он вызовет

tavilyClient.search(query)

и вернет уже подготовленный текст модели.

Этап 5

Подключить Tool в AiServiceConfig

AiServices.builder(ResearchAgent.class)
        .chatLanguageModel(model)
        .tools(searchTool)
        .build();

После этого Gemini сможет самостоятельно вызывать поиск.

Этап 6

Доработать системный промпт Research Agent.

Сейчас там написано просто "ты исследователь".

Я бы изменил его примерно так:

If you need recent information,
framework versions,
documentation,
API references,
best practices,
or any information that may have changed over time,
ALWAYS use the Search Tool first.

Do not rely solely on your internal knowledge when fresh information is required.

Это значительно повысит качество исследований.

Что предлагаю дальше

Я бы не стал сразу писать весь код интеграции одним большим сообщением.

Лучше сделать это поэтапно, как в настоящем проекте:

DTO Tavily (request/response) — около 10–15 минут.
TavilyClient через современный RestClient из Spring Boot 3.
SearchTool для LangChain4j.
Подключение Tool к ResearchAgent.
Тестирование запросом вроде: "Найди последние изменения в Spring Boot 3.5 и подготовь рекомендации".

Так мы получим чистую, расширяемую интеграцию, а не набор кода, который потом придется переделывать.

SearchTool продолжаем отсюда

Отлично, продолжаем. Мы уже договорились, что архитектура будет такой:

ResearchAgent
       │
       ▼
SearchTool
       │
       ▼
TavilyClient
       │
       ▼
Tavily API

То есть ResearchAgent ничего не знает о Tavily. Он знает только, что существует инструмент поиска.

Шаг 1. Создаем SearchTool

Пакет:

tool
└── SearchTool.java
package com.example.multiagent.tool;

import com.example.multiagent.integration.tavily.TavilyClient;
import com.example.multiagent.integration.tavily.dto.TavilyResponse;
import dev.langchain4j.agent.tool.Tool;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Component;

@Component
@RequiredArgsConstructor
public class SearchTool {

    private final TavilyClient tavilyClient;

    @Tool("""
            Search the Internet for recent technical information.
            
            Use this tool when you need:
            - latest framework versions
            - official documentation
            - API references
            - best practices
            - recent changes
            - library documentation
            
            Return only relevant information.
            """)
    public String search(String query) {

        TavilyResponse response = tavilyClient.search(query);

        return formatResponse(response);
    }

    private String formatResponse(TavilyResponse response) {

        StringBuilder builder = new StringBuilder();

        response.results().forEach(result -> {

            builder.append("Title: ")
                    .append(result.title())
                    .append("\n");

            builder.append("URL: ")
                    .append(result.url())
                    .append("\n");

            builder.append("Content:\n")
                    .append(result.content())
                    .append("\n\n");

        });

        return builder.toString();
    }

}
Почему именно так?

Многие новички делают так:

return response.toString();

или

return objectMapper.writeValueAsString(response);

Это плохая идея.

LLM не нужен весь JSON.

Например Tavily может вернуть:

{
  "query": "...",
  "answer": "...",
  "response_time": 0.95,
  "images": [],
  "results": [...]
}

Большая часть этого мусора модели не нужна.

Лучше сразу превратить ответ в читаемый текст.

Например:

Title: Spring Boot 3.5 Release Notes

URL:
https://spring.io/...

Content:
Spring Boot 3.5 introduces...

Именно такой формат LLM понимает лучше.

Еще лучше

Я бы немного ограничил размер текста.

Например

private static final int MAX_CONTENT_LENGTH = 1200;

И

private String truncate(String text) {

    if (text == null) {
        return "";
    }

    if (text.length() <= MAX_CONTENT_LENGTH) {
        return text;
    }

    return text.substring(0, MAX_CONTENT_LENGTH) + "...";
}

Тогда в formatResponse():

builder.append(
        truncate(result.content())
);

Иначе Tavily может вернуть десятки тысяч символов, что быстро "съест" контекст модели.

Логирование

Я бы сразу добавил:

@Slf4j

И внутри метода:

log.info("Searching Tavily: {}", query);

После получения ответа:

log.info("Found {} search results",
        response.results().size());

Потом это очень поможет при отладке.

Следующий шаг

Теперь остается подключить SearchTool к ResearchAgent.

До этого у нас было:

@Bean
public ResearchAgent researchAgent(
        @Qualifier("researchModel")
        ChatLanguageModel model
) {

    return AiServices.builder(ResearchAgent.class)
            .chatLanguageModel(model)
            .build();
}

После добавления инструмента станет:

@Bean
public ResearchAgent researchAgent(
        @Qualifier("researchModel")
        ChatLanguageModel model,
        SearchTool searchTool
) {

    return AiServices.builder(ResearchAgent.class)
            .chatLanguageModel(model)
            .tools(searchTool)
            .build();
}

Вот после этого произойдет настоящая магия LangChain4j — Gemini сама будет решать, когда ей нужно вызвать search(), а когда достаточно собственных знаний. Именно это превращает обычный AI-сервис в полноценного агента с доступом к внешним инструментам.

выведи полный код под researchagent

Да. Ниже я покажу полностью весь код, который относится к ResearchAgent. После этого у тебя получится полностью рабочий исследовательский агент с поиском через Tavily.

Структура будет такой:

research
│
├── agent
│   └── ResearchAgent.java
│
├── tool
│   └── SearchTool.java
│
├── integration
│   └── tavily
│       ├── TavilyClient.java
│       ├── TavilyConfig.java
│       ├── dto
│       │   ├── TavilyRequest.java
│       │   ├── TavilyResponse.java
│       │   └── TavilyResult.java
│
├── config
│   └── AiServiceConfig.java
│
└── resources
    └── prompts
        └── research-system.txt
1. ResearchAgent.java
package com.example.multiagent.agent;

import dev.langchain4j.service.SystemMessage;
import dev.langchain4j.service.UserMessage;

public interface ResearchAgent {

    @SystemMessage(fromResource = "prompts/research-system.txt")
    String research(

            @UserMessage
            String request

    );

}
2. research-system.txt
You are a senior software architect and technical researcher.

Your primary responsibility is to collect accurate and up-to-date technical information before implementation.

You have access to a Search Tool.

IMPORTANT:

If the request contains anything related to:

- latest versions
- documentation
- APIs
- frameworks
- libraries
- best practices
- tutorials
- recent changes
- release notes

ALWAYS use the Search Tool first.

Do not rely only on your internal knowledge when fresh information may be required.

Your task is to produce a concise technical report.

The report should contain:

1. Technologies to use
2. Architecture recommendations
3. Libraries
4. Best practices
5. Potential pitfalls

Do NOT generate implementation code.
Return only the research report.
3. SearchTool.java
package com.example.multiagent.tool;

import com.example.multiagent.integration.tavily.TavilyClient;
import com.example.multiagent.integration.tavily.dto.TavilyResponse;
import dev.langchain4j.agent.tool.Tool;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

@Slf4j
@Component
@RequiredArgsConstructor
public class SearchTool {

    private static final int MAX_CONTENT_LENGTH = 1200;

    private final TavilyClient tavilyClient;

    @Tool("""
            Search the Internet for recent technical information.

            Use this tool whenever you need:
            - documentation
            - framework versions
            - API references
            - release notes
            - best practices
            - recent information

            Return only relevant information.
            """)
    public String search(String query) {

        log.info("Searching Tavily: {}", query);

        TavilyResponse response = tavilyClient.search(query);

        log.info("Found {} results", response.results().size());

        return formatResponse(response);
    }

    private String formatResponse(TavilyResponse response) {

        StringBuilder builder = new StringBuilder();

        response.results().forEach(result -> {

            builder.append("Title: ")
                    .append(result.title())
                    .append("\n");

            builder.append("URL: ")
                    .append(result.url())
                    .append("\n");

            builder.append("Content:\n")
                    .append(truncate(result.content()))
                    .append("\n\n");

        });

        return builder.toString();
    }

    private String truncate(String text) {

        if (text == null) {
            return "";
        }

        if (text.length() <= MAX_CONTENT_LENGTH) {
            return text;
        }

        return text.substring(0, MAX_CONTENT_LENGTH) + "...";
    }

}
4. TavilyRequest.java
package com.example.multiagent.integration.tavily.dto;

public record TavilyRequest(

        String api_key,

        String query,

        int max_results

) {
}
5. TavilyResult.java
package com.example.multiagent.integration.tavily.dto;

public record TavilyResult(

        String title,

        String url,

        String content

) {
}
6. TavilyResponse.java
package com.example.multiagent.integration.tavily.dto;

import java.util.List;

public record TavilyResponse(

        List<TavilyResult> results

) {
}
7. TavilyClient.java
package com.example.multiagent.integration.tavily;

import com.example.multiagent.integration.tavily.dto.TavilyRequest;
import com.example.multiagent.integration.tavily.dto.TavilyResponse;
import lombok.RequiredArgsConstructor;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;
import org.springframework.web.client.RestClient;

@Component
@RequiredArgsConstructor
public class TavilyClient {

    private final RestClient restClient;

    @Value("${tavily.api-key}")
    private String apiKey;

    public TavilyResponse search(String query) {

        TavilyRequest request = new TavilyRequest(
                apiKey,
                query,
                5
        );

        return restClient.post()
                .uri("/search")
                .body(request)
                .retrieve()
                .body(TavilyResponse.class);
    }

}
8. TavilyConfig.java
package com.example.multiagent.integration.tavily;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.client.RestClient;

@Configuration
public class TavilyConfig {

    @Bean
    public RestClient restClient(

            @Value("${tavily.base-url}")
            String baseUrl

    ) {

        return RestClient.builder()
                .baseUrl(baseUrl)
                .build();
    }

}
9. application.yml
tavily:
  api-key: ${TAVILY_API_KEY}
  base-url: https://api.tavily.com
10. AiServiceConfig

Добавляем SearchTool.

@Bean
public ResearchAgent researchAgent(

        @Qualifier("researchModel")
        ChatLanguageModel model,

        SearchTool searchTool

) {

    return AiServices.builder(ResearchAgent.class)
            .chatLanguageModel(model)
            .tools(searchTool)
            .build();

}
Что получится в итоге

Теперь ResearchAgent будет работать по следующему сценарию:

Запрос пользователя
        │
        ▼
ResearchAgent
        │
        ▼
LLM анализирует запрос
        │
        ├────────► нужен поиск?
        │
       Да
        │
        ▼
SearchTool.search(...)
        │
        ▼
Tavily API
        │
        ▼
Результаты поиска
        │
        ▼
LLM анализирует найденную информацию
        │
        ▼
Research Report
Небольшое улучшение

Я бы еще внес одно изменение в SearchTool: вместо возврата одной большой строки возвращал бы Markdown-отчет (с заголовками, списками и ссылками). LLM обычно воспринимают такой формат лучше, чем простой конкатенированный текст, что положительно влияет на качество итогового исследования.